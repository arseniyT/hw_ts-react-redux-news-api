import React from "react";

const Preloader = () => {
    return (
        <div className="preloader">
            <span className="loader">
                8)
            </span>
        </div>
    );
};

export default Preloader;
